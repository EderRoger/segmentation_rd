<%= form_for(@query) do |f| %>
  <% if @query.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @query.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :condition %><br>
    <%= f.text_field :condition %>
  </div>
  <div class="field">
    <%= f.label "Field" %><br>
    <%= f.select(:field, options_for_select(QueryBuilder::AVAILABLE_FIELDS)) %>
  </div>
  <div class="field">
    <%= f.label "Operator" %><br>
    <%= f.select(:operator, options_for_select(QueryBuilder::AVAILABLE_OPERATORS)) %>
  </div>
 <div class="field">
    <%= f.label "Value" %><br>
    <%= f.text_field :value %>
  </div>

  <div class="Agregator">
    <%= f.label "Agregator" %><br>
    <%= f.select(:agregator, options_for_select(QueryBuilder::AGREGATORS), {include_blank: 'None'}) %>
  </div>
  </br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
